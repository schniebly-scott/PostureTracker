# Codex project instructions

`claude.md` is the canonical project guide shared by Claude and Codex.

- Read `claude.md` in full before inspecting, planning, reviewing, or changing this project.
- Follow its architecture notes, conventions, testing guidance, and platform constraints.
- Keep durable project guidance in `claude.md`; do not duplicate it here.
- Ignore `.claude/worktrees/` during routine searches and project analysis unless the user
  explicitly asks to inspect a Claude worktree.
- Treat `.claude/settings.local.json` as Claude-specific permission metadata. Continue to
  honor the active Codex sandbox and approval policy instead of copying Claude command
  allowlists.
